{
  "name": "learntime",
  "version": "0.1.0",
  "private": true,
  "description": "LearnTime desktop app (Linux/Windows) using Tauri 2",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
